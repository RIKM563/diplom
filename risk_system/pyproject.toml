[project]
name = "risk_system"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "pydantic>=2.6",
  "numpy>=1.26",
  "pandas>=2.2",
  "networkx>=3.2",
]

[tool.ruff]
line-length = 100